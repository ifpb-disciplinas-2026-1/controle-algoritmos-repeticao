// O que esse algoritmo faz?
#include <stdio.h>
int main(){
  int numero, contador=1;
  // printf("%i", numero);
  while (numero!=contador){
     printf("Digite um número: ");
     scanf("%i", &numero);
  }
return 0; 
}